{
  "database": {
    "rules": {
      "appUpdates": {
        "windows": {
          "latest": {
            ".read": true,
            ".write": "auth != null && root.child('users').child(auth.uid).child('isAdmin').val() === true"
          }
        }
      }
    }
  }
}
